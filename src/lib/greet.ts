/**
 * Generates a greeting message.
 * This is a simple utility used for initial project verification.
 */
export function greet(name: string): string {
  return `Hello, ${name}! Welcome to HAAL Installer.`;
}
